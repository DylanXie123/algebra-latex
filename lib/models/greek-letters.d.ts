export declare const letters: {
    name: string;
    symbol: string;
}[];
export declare function toUpperCase(x: string): string;
export declare function isUpperCase(x: string): boolean;
export declare function getSymbol(name: string): string | null;
export declare function getName(symbol: string): string | null;
export declare function convertSymbols(math: string): string;
export default letters;
